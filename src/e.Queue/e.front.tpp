// ============================================================================
// Получение первого элемента без удаления
// ============================================================================
template <typename T>
T Queue<T>::front() {
  if (isEmpty()) {
    throw std::out_of_range("Queue is empty.");
  }
  return front_->data_;
}
